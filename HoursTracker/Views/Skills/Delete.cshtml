@model HoursTracker.Models.Skill

@{
    ViewData["Title"] = "Xóa Kỹ năng";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Xác nhận Xóa</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <strong>Cảnh báo!</strong> Bạn có chắc chắn muốn xóa kỹ năng này? Tất cả các log luyện tập liên quan cũng sẽ bị xóa.
                    </div>

                    <dl class="row">
                        <dt class="col-sm-4">Tên kỹ năng:</dt>
                        <dd class="col-sm-8">@Model.Name</dd>

                        <dt class="col-sm-4">Tổng giờ:</dt>
                        <dd class="col-sm-8">@Model.TotalHours giờ</dd>

                        <dt class="col-sm-4">Số log:</dt>
                        <dd class="col-sm-8">@Model.PracticeLogs.Count</dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Hủy
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Xóa
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

