@model HoursTracker.Models.PracticeLog

@{
    ViewData["Title"] = "Thêm Log Luyện tập";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-plus-circle"></i> Thêm Log Luyện tập</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" id="practiceLogForm">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="SkillId" class="form-label"></label>
                            <select asp-for="SkillId" class="form-select" asp-items="ViewBag.SkillId">
                                <option value="">-- Chọn kỹ năng --</option>
                            </select>
                            <span asp-validation-for="SkillId" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="PracticeDate" class="form-label"></label>
                                <input type="date" asp-for="PracticeDate" class="form-control" />
                                <span asp-validation-for="PracticeDate" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label asp-for="Minutes" class="form-label"></label>
                                <input type="number" asp-for="Minutes" class="form-control" min="1" max="1440" placeholder="Số phút" />
                                <span asp-validation-for="Minutes" class="text-danger"></span>
                                <small class="form-text text-muted">Tối đa 1440 phút (24 giờ)</small>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Notes" class="form-label"></label>
                            <textarea asp-for="Notes" class="form-control" rows="4" placeholder="Ghi chú về buổi luyện tập..."></textarea>
                            <span asp-validation-for="Notes" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="javascript:history.back()" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle"></i> Lưu Log
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Milestone Celebration Modal -->
<div class="modal fade" id="milestoneModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-body text-center p-5">
                <div class="celebration-animation">
                    <i class="bi bi-trophy-fill text-warning" style="font-size: 5rem;"></i>
                </div>
                <h2 class="mt-4 mb-3">🎉 Chúc mừng! 🎉</h2>
                <p class="lead" id="milestoneMessage"></p>
                <button type="button" class="btn btn-primary btn-lg mt-3" data-bs-dismiss="modal">
                    Tuyệt vời!
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/practice-log-form.js"></script>
}

